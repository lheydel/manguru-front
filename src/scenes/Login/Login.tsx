import React from 'react';
import LoginContent from './LoginContent';

/**
 * User login page with its content and all the general elements (header, footer, ...)
 */
export const Login: React.FC = () => (
  <LoginContent />
);
