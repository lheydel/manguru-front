import React from 'react';
import RegisterContent from './RegisterContent';

/**
 * User registration page with its content and all the general elements (header, footer, ...)
 */
export const Register: React.FC = () => (
  <RegisterContent />
);
