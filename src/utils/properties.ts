export enum Language {
    DEFAULT = 'en',
    EN = 'en',
    FR = 'fr',
}

export enum RouteFront {
    HOME = '/',
    LOGIN = '/login',
    REGISTER = '/register',
}

export enum RouteBack {
    LOGIN = '/login',
    USER = '/user',
}

export enum Cookie {
    AUTH = 'Authorization',
}
